<div class="col-sm-6">
	<h2>Register</h2>
	<%= simple_form_for(@user) do |f| %>
		<%= f.input :name %>
		<%= f.input :email %>
		<%= f.input :password %>
		<%= f.input :password_confirmation %>
		<%= f.submit 'Log In', class: 'btn btn-primary' %>
	<% end %>
</div>